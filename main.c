# include <stdio.h>
int main() {
    unsigned int numCity = 0;
    printf("Input number of city:");
    scanf("%d", &numCity);
    printf("%d cities in total.\n", numCity);

    int adjacencyMatrix[numCity][numCity];
}
